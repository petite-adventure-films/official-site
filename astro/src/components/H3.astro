---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<h3 class:list={['text-base font-bold', className]}>
  <span><slot /></span>
  {
    Astro.slots.has('sub') && (
      <span class="block text-xs">
        <slot name="sub" />
      </span>
    )
  }
</h3>
