ErrorDocument 403 /403.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_URI} !/403.html
  RewriteCond %{REQUEST_FILENAME} !^(.*)\.(jpg|png|css)$
  RewriteCond %{REMOTE_ADDR} !138.64.83.100
  RewriteRule ^.*$ - [R=403,L]
</IfModule>
